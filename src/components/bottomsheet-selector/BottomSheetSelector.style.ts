import { StyleSheet } from 'react-native';

const styles =
  StyleSheet.create({
    bottomSheetStyle: {
      flex: 1,
      paddingBottom: 16,
    },
    radioButtonContainer: {
      marginTop: 8,
    },
    radioButtonStyle: {
      flexDirection: 'row',
      alignItems: 'center',
      paddingHorizontal: 8,
    },
    applyButtonStyle: {
      marginHorizontal: 16,
    },
  });

export default styles;
